services:
  mcp-gaming:
    build: .
    container_name: sphere-mcp-gaming
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    stdin_open: true
    tty: true
